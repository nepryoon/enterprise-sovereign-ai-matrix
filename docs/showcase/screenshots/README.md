# Rendered showcase evidence

This directory is reserved for deterministic Playwright captures at the documented viewports.
No screenshot is committed in this change because the execution environment contained no browser
binary and blocked package/browser downloads. Add evidence only after rendering the exact commit;
record the command, viewport, scenario, and commit hash in `docs/design/VISUAL_QA_REPORT.md`.
