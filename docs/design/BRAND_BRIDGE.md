# Neuromorphic Inference Lab brand bridge

## Source and access record

The owner identifies `style.css` and the home shell in
`nepryoon/neuromorphic-inference-lab-site` as canonical and explicitly excludes `nil.css`.
On 2026-08-23 this environment could not clone GitHub or reach either live site: the proxy returned
HTTP 403. Consequently the source commit and asset hashes could not be verified, and `logo.svg`
and `favicon.svg` are not copied or represented as verified brand assets in this change. This is a
remaining owner-review action; hotlinking or inventing replacements would be less truthful.

## Normalised local tokens

| Semantic token | Value |
| --- | --- |
| Canvas 0 / Canvas 1 | `#070A12` / `#0B1220` |
| Primary / muted text | `#EAF0FF` / `rgba(234,240,255,.72)` |
| Cyan / violet / emerald | `#6EE7FF` / `#A78BFA` / `#34D399` |
| Base / elevated surface | `rgba(255,255,255,.05)` / `.07` |
| Border | `rgba(255,255,255,.12)` |
| Main shadow | `0 12px 40px rgba(0,0,0,.35)` |
| Major / control radius | `18px` / `12px` |

The operational adaptation uses system sans, monospace identifiers/metrics, restrained radial
cyan/violet/emerald light, glass borders, cyan-violet primary actions, emerald health, and cyan
focus rings. It deliberately removes marketing-width constraints and limits glow to activity or
selection.

## Asset completion procedure

After restoring GitHub access, an owner should copy the exact canonical `logo.svg` and
`favicon.svg` into `apps/web/public`, record the brand commit and SHA-256 hashes here, run the
licence/provenance review, then execute the frontend visual and accessibility gates.

