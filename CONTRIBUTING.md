# Contributing

Use English, create an issue, branch from `main`, and keep commits conventional and cohesive. Install with `make setup`; before opening a PR run `make ci`. Add deterministic tests for behaviour and update ADRs/docs when architecture, security or cost changes. Never enable paid model calls in tests or commit secrets/generated artefacts. Pull requests must describe risk, validation, rollback and screenshots for visible UI changes.
