export type ExecutionStatus = "QUEUED" | "RUNNING" | "WAITING_APPROVAL" | "COMPLETED" | "FAILED" | "CANCELLED";
export type ModelClass = "FAST" | "BALANCED" | "REASONING" | "SOVEREIGN";
export interface TelemetryEvent { schema_version: string; event_id: string; sequence: number; execution_id: string; correlation_id: string; timestamp: string; event_type: string; agent_id: string | null; status: string | null; model_class: ModelClass | null; provider: string | null; model?: string | null; placement?: string | null; route_reason?: string | null; fallback_allowed?: boolean | null; policy_outcome?: string | null; message_kind?: string | null; message?: string | null; recipient?: string | null; evidence_refs?: string[]; latency_ms: number | null; prompt_tokens: number | null; completion_tokens: number | null; estimated_cost_eur: number | null; trace_id: string | null; metadata?: Record<string, unknown>; }
export interface AgentView { id: string; role: string; status: string; modelClass: ModelClass | null; provider: string | null; model: string | null; placement: string | null; routeReason: string | null; fallbackAllowed: boolean | null; policyOutcome: string | null; latency: number; promptTokens: number; completionTokens: number; cost: number; outcome: string; }
export interface Execution { request?: string; scenario?: string; result?: string|null; error?: string|null; execution_id: string; correlation_id: string; status: ExecutionStatus; risk_level: string; data_sensitivity: string; current_node: string; created_at: string; updated_at: string; trace_id?: string | null; requested_action?: string; approval_reason?: string; evidence?: string[]; recommended_decision?: string; }
export interface DashboardState { execution: Execution | null; events: TelemetryEvent[]; agents: Record<string, AgentView>; connection: "idle" | "connecting" | "connected" | "reconnecting" | "error"; error: string | null; }
